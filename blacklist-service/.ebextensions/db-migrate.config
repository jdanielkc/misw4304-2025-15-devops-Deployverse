container_commands:
  01_migrate:
    command: "python3 -c 'from application import application; from src.database import db; app_context = application.app_context(); app_context.push(); db.create_all(); app_context.pop(); print(\"Tablas creadas exitosamente\")'"
    leader_only: true